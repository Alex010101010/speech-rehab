# Бэклог — speech-rehab

Кандидаты из ревью по бест-практис реабилитационных приложений (Tactus Therapy, Constant Therapy, retrieval practice, WCAG, обзоры адхеренса) + фидбек методиста. Сортировка 🔴→🟡→🟢. Сделанное отслеживается в git-истории, здесь не дублируется.

| Задача | Чем критична | Приоритет | Объём |
|---|---|---|---|
| Иерархия подсказок (cueing hierarchy) + лог уровня подсказки | Сейчас подсказка бинарна (подсказка/«не знаю»). Доказательно — ступенчатые подсказки от слабой к сильной (значение→категория→рифма→первый звук→достраивание фразы→образец) и запись, на какой ступени пациент справился: это и помощь, и измеримая метрика восстановления (Constant Therapy показывает снижение числа подсказок во времени). Применить к называнию (`name_by_description`, `picture_word`). Код (APK), тексты подсказок по OTA. **Связь с интервальным повторением:** сейчас в spaced retrieval засевается только успех `unaided`; когда появятся ступени подсказок — «справился на ступени N» тоже кандидат на засев (сейчас такое слово в расписание не попадает). **Сделано:** для `name_by_description` (печатный ввод) лог глубины подсказки `cueLevel` 0–3 (0 сам → 1 первый звук → 2 больше букв → 3 показан ответ) персистится посессионно в `history` (`nameCueSum`/`nameCueN`) — материал для метрики «снижение числа подсказок». **Остаётся:** семантические ступени (категория/рифма — нужен авторинг контента + ревью логопеда), применение к `picture_word`, вывод метрики в отчёт. Ист.: tactustherapy cueing-hierarchy, Constant Therapy, PMC5285333 **Постановка (объём B, 14.07):** берём разблокированное + механизм семантики; правку spaced-repeat и UI отчёта НЕ трогаем (отдельные задачи). Модель — две независимые оси: фонематическая `cueLevel` 0–3 (без изменений, история сопоставима) и новая смысловая подсказка отдельным полем. *Как сейчас:* `TypedExercise._outcome` (`exercises.dart:1046`) пишет `cueLevel = _revealed?3:_letterHints`; `session_screen._onOutcome` (`session_screen.dart:356`) копит `nameCueSum/nameCueN` только для `name_by_description`; `PictureWordExercise._outcome` (`exercises.dart:483`) `cueLevel` не выставляет (всегда 0); засев в spaced-repeat (`_maybeSeedReview`, `session_screen.dart:308`) только при `unaided`. *Шаги (по вероятности переделки):* (1) модель `StepOutcome` (`models/exercise.dart:26`) — добавить поле `semanticCue` (int, дефолт 0; пока 0/1 «использована смысловая подсказка», финер-градация категория/рифма — в content-follow-up) → проверка: дефолт не ломает существующие вызовы конструктора StepOutcome. (2) `session_screen._onOutcome` — отдельный аккумулятор `_recogCueSum/_recogCueN` для `picture_word` (по аналогии с nameCue) + счётчик `semanticCue` из `o.semanticCue`; писать в `history` рядом с nameCue, только если N>0 → проверка: снимок сессии содержит новые поля лишь при непустых счётчиках, лимит 60 сохраняется. (3) `PictureWordExercise._outcome` — своя лесенка узнавания: 0 сам, 1 погасили дистрактор ИЛИ была неверная попытка (`_hintUsed||_wrongPick!=null`), 3 показан ответ (`_revealed`) → в ОТДЕЛЬНЫЙ бакет recogCue, НЕ в nameCue → проверка: три пути дают cueLevel 0/1/3. (4) `TypedExercise` — закрыть дыру: раскрытие картинки/эмодзи (`_giveHint`, `exercises.dart:1014`) сейчас ставит `_hintUsed`, но `cueLevel` не меняет → успех с картинкой логируется как 0; по двухосевой модели картинка = смысловая подсказка → выставлять `semanticCue=1`, фонематический `cueLevel` не трогать → проверка: показ картинки + верный ответ даёт `cueLevel=0, semanticCue=1`. (5) механизм семантических ступеней — опциональное поле контента `semHint` (строка: категория/рифма) в item; в `_giveHint` добавить смысловую ступень ПЕРЕД буквенными (слабее→сильнее), при показе — `semanticCue=1`; контент пустой (наполнение + ревью логопеда = follow-up) → проверка: без `semHint` поведение как раньше, с `semHint` появляется смысловая кнопка-ступень. *Развилки (решено):* объём=B (движок+телеметрия+механизм семантики; без правки расписания и UI отчёта — они переопределены соседними задачами); шкала=две оси (cueLevel фонематический без ремапа + отдельное `semanticCue` → история `nameCueSum` сопоставима, нет разрыва тренда); picture_word=отдельный бакет `recogCueSum/recogCueN` (узнавание ≠ продукция, не усредняем разные конструкты). *Зависимости:* вывод трендов в интерфейс — задача «Отчёт о прогрессе для близких/логопеда»; наполнение `semHint` + ревью логопеда — content-follow-up (OTA); засев cued-успехов в spaced-repeat — отдельная задача (правка ядра обучения). *Критерий:* widget-тесты в `app/test/` (CI `flutter test`): PictureWordExercise эмитит cueLevel 0/1/3 по путям; TypedExercise с показанной картинкой даёт `semanticCue=1, cueLevel=0`; чистый хелпер аккумуляции (вынести из session_screen) раскладывает исходы по бакетам name/recog/sem; красный кейс — наивная реализация (picture_word cueLevel всегда 0; картинка не считается подсказкой) тесты валит. Код в `app/lib/**` → нужна пересборка APK, НЕ OTA (кроме будущего `semHint`-контента). **Реализовано 12.09.2026 (объём B, не запушено — карточка открыта до пуша и сборки APK):** `StepOutcome.semanticCue` — вторая, смысловая ось (картинка/`semHint`), фонематический `cueLevel` не тронут, история `nameCueSum` сопоставима. `PictureWordExercise` получил свою лесенку узнавания (0 сам / 1 погашен дистрактор или неверная попытка / 3 показан ответ) в ОТДЕЛЬНЫЙ бакет `recogCueSum`/`recogCueN`. Закрыта дыра `TypedExercise`: успех с раскрытой картинкой больше не логируется как «справился сам» (`semanticCue=1`). Аккумуляция вынесена в чистый `CueTally` (`models/exercise.dart`), в снимок сессии идут только непустые бакеты (+ `semCueN`). Механизм `semHint` в движке есть, контент пустой. Тесты: `app/test/cue_hierarchy_test.dart`, 25 зелёных локально, наивная реализация их валит. **Остаётся к закрытию:** пуш + APK на планшет; вывод трендов в отчёт — отдельная карточка. | 🔴 | M |
| Наполнить контент семантических подсказок (`semHint`) + ревью логопеда | Follow-up механизма иерархии подсказок (шаг 5 той задачи): движок будет показывать смысловую ступень (категория/рифма) перед фонематическими, но поле `semHint` в контенте пустое — без наполнения ступень не появляется. Нужны тексты смысловых подсказок к называнию (`name_by_description`) и, где уместно, `picture_word`: категория («это посуда»), рифма, функция предмета. Ревью логопедом обязательно — клиническая корректность формулировок (какая подсказка «слабее»/«сильнее», уместность для афазии). Чисто контент (OTA, без APK). **Готово в движке (12.09.2026):** поле `semHint` уже читается `TypedExercise` и показывается как самая слабая ступень подсказки перед картинкой и буквами; осталось наполнить контент. Уедет на планшет только после сборки APK с этим кодом. Ист.: tactustherapy cueing-hierarchy | 🟡 | M |
| Засев cued-успехов в интервальный повтор (spaced retrieval) | Сейчас `_maybeSeedReview` (`session_screen.dart:308`) заводит карточку в расписание только при `unaided` (справился сам, без подсказок). Слово, решённое с подсказкой (`cueLevel>0` или `semanticCue`), в повтор НЕ попадает — а именно оно и нуждается в закреплении. Когда появились ступени подсказок, «справился на ступени N» — тоже кандидат на засев, но с более ранним/низким `box` (вернётся раньше, чем unaided-успех, но без «долбёжа» в той же сессии). Правка ядра обучения (`ReviewScheduler`/box-логика) клинически чувствительна — делать аккуратно, с тестами в `app/test/`. Код (APK). **Разблокировано (12.09.2026):** телеметрия готова — `cueLevel` эмитят и `TypedExercise`, и `PictureWordExercise`, смысловые подсказки лежат в `semanticCue`. Ист.: retrieval practice, Constant Therapy | 🟡 | S-M |
| Функциональный перенос: мини-скрипты бытовых ситуаций | RCT-вывод: тренировка называния НЕ переносится на реальное общение — дрилл сам по себе не даёт разговорной речи. Нужны задания на употребление слова в контексте: бытовые диалоги (приветствие, телефон, аптека, заказ), достраивание до целой реплики; формат Cuespeak «вопрос → облегчённый ответ» с closure-подсказками. Частично пересекается со строкой «`complete_phrase_choice` завершениями-фразами». В основном контент (OTA), возможно новый тип. Ист.: NBK556720 (Big CACTUS RCT), Cuespeak | 🟡 | M-L |
| Петля приверженности для самоучки 70+ | Больше дней практики в неделю → больше прирост (когорта Constant Therapy, 2249 чел.). Пожилых мотивирует «осмысленное» применимое в жизни, рутина, цели, понятная обратная связь, персональный словарь — не абстрактные очки и без инфантилизации. Добавить: дневную серию, мягкое напоминание по расписанию, экран прогресса (календарь активности), возможность близкому добавить личные слова/фото пациента. Код + контент-пайплайн. **Связь с интервальным повторением:** расписание повторов (1д→2д→5д→14д) даёт эффект, только если пациент возвращается по графику — без напоминания интервалы плывут; делать вместе. Ист.: PMC9804200 (Constant Therapy cohort), PMC10012313 | 🟡 | M |
| Отчёт о прогрессе для близких/логопеда | Вернуть скрытый «Отчёт логопеду» и оформить лаконичную сводку: точность по типам, динамика, серия дней (эталон — дашборд Constant Therapy: accuracy, скорость/латентность, использованные подсказки, «на треке ли»). Шарить близкому. **План «показать один раз» (данные уже копятся в фоне, отчёт не трогали):** (1) раскрыть плитку «Отчёт для логопеда» (`home_screen.dart:176`, код `_buildReport`/`_showReport` цел); (2) добавить **кривую удержания / «сколько слов закреплено»** из `progress.review` (число карточек, распределение по коробкам/стадиям); (3) добавить тренд **«среднее число подсказок»** из `history` — теперь три раздельных бакета (задача иерархии подсказок разложила): `nameCueSum`/`nameCueN` (фонематическая глубина называния), `recogCueSum`/`recogCueN` (узнавание, `picture_word`), счётчик `semanticCue` (смысловые подсказки); все по сессиям, должны падать со временем, бенчмарк Constant Therapy. Показывать раздельно — узнавание ≠ продукция. Код, без OTA. Ист.: Constant Therapy clinician dashboard | 🟡 | M |
| Аудит доступности по обзору npj Digital Medicine (2025) | Чек-лист: крупные явно кликабельные кнопки, экран ≥7″, уровень текста ≤5 класса, повседневный язык вместо терминов, ≤3 экранов текста, единый шрифт, иллюстрации для снижения когнитивной нагрузки. Мы во многом уже там — пройтись чек-листом (контраст, размер целей, длина инструкций). ⚠️ обзор таргетит молодых <55, применять с поправкой на 70+. Ист.: nature s41746-025-01796-8 | 🟢 | S |
| Лёгкий пол контента (тир легче L1): договаривание словосочетаний с жёстким контекстом | Сейчас для текстовых типов нет тира проще L1. Узнавание + растормаживание — самый доступный вход (грубая форма). Банк-кандидатов — ниже под таблицей. Сделано: слоги, да/нет слово↔картинка, картинка→слово, соединить пары (match_pairs), автоматизир. ряды (auto_series), выбор формы слова касанием (endings_choice, ревью логопедом ✅) | 🟡 | M |
| Расширить `complete_phrase_choice` завершениями-фразами (не одним словом) | Сейчас варианты = одно слово (удочку/лопату/молоток). Добавить задания, где завершение — целая клауза: «Он пошёл на реку, чтобы… [половить рыбу / починить забор / купить хлеб]». **Переиспользуем существующий тип/виджет** (ChoiceExercise) → только контент, едет по OTA без APK. Ревью логопедом | 🟡 | M |
| Семантическая difficulty-разметка обобщений логопедом | Сложность по смыслу, не по числу слов; бот разметил 7 явно абстрактных на L3 черновиком, нужна полная проверка | 🟡 | M |
| Сократить ответы текстов чтения на L2/L3 до коротких фраз (L1 сделано) | После каждого текста в `reading_texts` (18_*) идут вопросы на понимание, к каждому — эталонный ответ (поле `a`), который показывается для самопроверки. На L1 эталоны уже короткие («Куда садится Пётр?» → «у окна»), а на L2 (7 текстов) и L3 (7 текстов) это всё ещё полные предложения, повторяющие текст дословно — припомнить их труднее. Нужно переписать 56 ответов L2/L3 в короткую фразу, сохранив суть. Пример: «Он отплыл к середине реки и закинул сети.» → «отплыл, закинул сети». Чисто контент (OTA, без APK), ревью логопедом | 🟢 | M |
| Наполнить `stress.json` ударениями по прослушиванию + подтвердить U+0301 на планшете | Словарь произношения заведён (content/stress.json, едет по OTA), стартовый набор почти не пересекается с контентом. На планшете: проверить, слушается ли движок знака U+0301 (на словах столяр/досуг/творог/змея), и пополнять словарь точными формами слов, где ударение звучит неверно. Найдено: змея→змея́. От U+0301 зависит, оставляем словарь или план Б (выбор голоса) | 🟡 | S |
| Advanced-модуль для лёгкой афазии (пословицы/паронимы/словообразование) | Вывести помеченные `advanced` типы как опциональный трек | 🟢 | M |
| STT как «запись + сравнение» (не строгое распознавание) | Голосовой ввод из THEORY; после инсульта строгое распознавание ошибается | v2 | L |
| word_formation (`11_*`): примеры/проверочные вопросы к части речи **во всём блоке** | wf_001 (block-wide): без примера неясно, в какую сторону словообразования думать (сущ./прил./глагол) | 🟡 | M |
| [UX] Фиксация горизонтального (landscape) режима. Сейчас в `app/lib/main.dart` ориентация не задаётся — на планшете не зафиксирована. Добавить `SystemChrome.setPreferredOrientations([landscapeLeft, landscapeRight])` в `main()` (import `package:flutter/services.dart`). Тривиально. | На планшете режим «гуляет», нет фиксации горизонтали | 🟡 | XS |
| [UX] Клавиатура перекрывает задание в режиме ввода (`TypedExercise`, `widgets/exercises.dart`). Контент урока в `ExerciseScaffold` (`Expanded → SingleChildScrollView`), `Scaffold` сжимает body, но на landscape-планшете мало высоты: картинка-подсказка 200px + промпт + шкала → поле ввода/задание уезжают под клавиатуру. Обсудить подход (не решено): (1) прокрутка к полю + резерв под клавиатуру (минимально); (2) прятать картинку/шкалу при открытой клавиатуре; (3) поле ввода закрепить снизу над клавиатурой (как мессенджер). Уточнить у пользователя желаемое поведение перед реализацией. | Во время набора ответа не видно задание — мешает пользоваться | 🟡 | S |
| [SECURITY] Включить 2FA на GitHub-аккаунте Alex010101010 | Keystore лежит в GitHub Secrets → компрометация аккаунта = возможность подписать вредоносный APK твоим ключом и раздать через самообновление (Tier B). 2FA закрывает основной риск. Не критично сейчас (аккаунт под контролем), но должно быть в планах. Где: github.com/settings/security | 🟢 | XS |
| [SECURITY] Запинить сторонние GitHub Actions на commit-SHA (subosito/flutter-action@v2, actions/checkout@v4 и т.п.) | Плавающий тег → теоретически скомпрометированный action прочитает секреты подписи в нашей же сборке. Гигиена для репо с keystore-секретами, не блокер | 🟢 | S |

---

## Банк лёгких упражнений (для облегчения — собираем)

> Отзыв пользователя: приложение «сложновато». Цель — собрать самый доступный тир заданий (узнавание + растормаживание, уровень грубой/средней формы). Источники: методичка МОНИКИ (Щербакова/Котов, 2014), презентация Гришаниной (острый период), Клепацкая «Восстановление речи после инсульта» (PDF в открытом доступе). Каждый кандидат — либо новый L0-материал к существующему типу, либо мини-режим.

| Упражнение | Почему легче | Куда лечь | Откуда брать контент |
|---|---|---|---|
| **Договаривание словосочетаний с жёстким контекстом** — носовой→платок, сливочное→масло, проливной→дождь, минеральная→вода, шариковая→ручка | Одно высокопредсказуемое слово, контекст ведёт сам | L0 к `complete_phrase_choice` / `complete_phrase_open` или warmup | МОНИКИ (эфф.-мот., афф.-мот.), Гришанина |
| **Договаривание пословиц с жёстким концом** (опц. с показанным образцом) — «Делу время, а потехе…», «Тише едешь — дальше…» | Конец однозначен; легче, чем объяснять смысл | L0 к `proverbs` (сейчас `proverbs` = объяснение, тяжелее) | МОНИКИ (афф.-мот.), Клепацкая |
| **Предмет по функции** — «на чём ездят?», «что едят?», «что носят?» | Категориальное узнавание проще описания признаков | L0 к `name_by_description` | МОНИКИ (акуст.-гност.: «определять предметы по функциям») |
| **Глобальное чтение** — подпись к картинке из 2 вариантов (не 3–4) | Меньше вариантов = ниже нагрузка | **OTA-готово**: `complete_phrase_choice`/`ChoiceExercise` рисует любое число опций → только контент | Гришанина (глобальное чтение), Клепацкая |

### Доработки движка/UX на будущее (облегчение)
- (v2, аудио) Пропевание знакомых фраз / Melodic Intonation Therapy — требует аудио-режима, не только TTS.

### Рассмотрено в ревью 28.06 и НЕ берём
- **Melodic Intonation Therapy (MIT)** — эффект ограниченный и временный, без переноса на функциональную речь; требует пения/аудио, а не TTS. (Совпадает с отложенным «аудио v2» выше.)
- **Constraint-Induced (CIAT/CILT)** — слабое превосходство над равными по интенсивности терапиями; требует «только говорить», что без распознавания речи мы не проконтролируем.

### Контент на будущее (низкий приоритет, отложено)
- Расширить пул коротких текстов чтения (`reading_texts`, добавлено 6 на L1, нужно больше для разнообразия) — floor не должен повторяться. Объём M, чисто контент (OTA).

### Новые типы на будущее (НЕ для облегчения — повышают сложность, отложены)
- **Логико-грамматические обороты** (пассив/реверсив «Петю ударил Ваня — кто драчун?», причинные «…потому что…») — центральный дефект средней афазии, подтверждён 3 источниками (МОНИКИ, Гришанина, Клепацкая). Высокая ценность, но это усложнение — делать после облегчающего тира.
- Возвратные конструкции (умываю/умываюсь); омонимы, метафоры, толкование-с-ошибкой — расширения для среднего/продвинутого уровня.
