# Flutter / Dart
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.packages
app/pubspec.lock

# Платформенные папки генерируются в CI (flutter create), в репо не храним
app/android/
app/ios/
app/web/
app/linux/
app/macos/
app/windows/
app/.metadata

# Вшитый снимок контента — производная от content/, генерится tools/build_assets.py
# (в CI перед flutter build). В git хранится только источник content/.
app/assets/content/

# Скачанные сборки (APK из CI-артефактов)
dist/

# IDE / OS
*.iml
.idea/
.vscode/
.DS_Store
