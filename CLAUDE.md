# Правила проекта speech-rehab

## Деплой: OTA vs APK — предупреждать в чате

**Перед деплоем явно предупреждать в чате, если правки требуют НЕ ТОЛЬКО OTA-обновления.**

- **OTA** (GitHub Pages `/ota`, без пересборки APK) доставляет на планшет **только контент**: `content/json/*.json`, `content/img/*`, `content/index.json` (при выпуске поднимать `content_version`).
- **НЕ доставляется по OTA** (нужен новый APK): любой код в `app/lib/**` (новые/изменённые виджеты, движок, экраны), `kAppOtaVersion`/`min_app_version`, зависимости `pubspec`.
- Поэтому: если в изменениях затронут `app/lib/**` → перед деплоем сказать пользователю «эти правки — код, OTA их не доставит, нужна пересборка APK (`gh workflow run build.yml -f build_apk=true`) и установка на планшет», и только потом деплоить.

## Ревью контента — только новые задания

**Никогда не выгружать на ревью весь банк (2000+ заданий).** Логопед/пользователь смотрит только новое.

- Ревью нового draft-набора — через `python3 tools/build_draft_review.py` → `tools/draft_review.html`. Страница показывает **только непросмотренные** задания (по которым ещё нет решения в `<тип>-decisions.json`).
- `tools/build_review.py` (полный список всех типов) — НЕ использовать для ревью нового; он для редких сплошных проверок.
- Подключение нового draft-типа к конвейеру ревью (3 точки): добавить набор в `GROUPS` в `build_draft_review.py`; завести префикс id и билдер в `apply_draft_decisions.py` (`PREFIX_FILE` + `REGEN`); билдер набора должен читать `<тип>-decisions.json` (убирать `approved:false`, снимать `draft`, когда по всем есть решение) — как `build_word_order.py`.
- Цикл: правки → `draft_review.html` → логопед сохраняет `draft-decisions.json` → `python3 tools/apply_draft_decisions.py <файл>` (разнесёт по пер-тип файлам, перегенерит наборы, пересоберёт ассеты, снимет `draft`).

## Id заданий — глобально уникальны по всем наборам

- Расписание интервального повторения (`progress_store.dart`, карта `review`) ключуется **только по id задания** → id должны быть уникальны не внутри набора, а **по всему контенту**. У каждого типа — свой префикс id (`ech_`, `pw_`, `pwh_`…), новый тип = новый префикс.
- Гейт в `tools/build_assets.py` валит сборку (локально и в CI) при дубле id между наборами.
- Прецедент: `part_whole` и `picture_word` оба использовали `pw_XXX` — переименовали в `pwh_XXX` (02.07.2026).

## Пуш и сборка — только по явному слову

- `git push` и сборку APK делать **только** по явному слову пользователя («пушим», «деплой», «собираем APK»). Не автоматически.
- Локального Flutter НЕТ → компиляцию Dart проверяет ТОЛЬКО CI (web-сборка на push). На push собирается только web; APK — вручную.
